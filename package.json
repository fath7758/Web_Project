{
  "name": "example_test",
  "version": "1.0.0",
  "description": "this is test to learn git and node",
  "main": "index.html",
  "scripts": {
    "start": "npm run lite",
    "lite" :"lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "fath",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
